{
  "name": "docker-graph",
  "version": "0.0.1",
  "browser": "src/ts/index.js",
  "repository": "github:Adirelle/docker-graph",
  "devDependencies": {
    "bun-types": "latest",
    "esbuild": "^0.14.51"
  },
  "dependencies": {
    "force-graph": "^1.42.11"
  },
  "scripts": {
    "build": "esbuild src/ts/index.ts --minify --bundle --platform=browser --outfile=public/js/index.js"
  }
}
